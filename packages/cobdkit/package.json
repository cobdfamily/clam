{
  "name": "@cobdfamily/cobdkit",
  "version": "0.0.0",
  "description": "Mini-app bridge runtime: shims navigator.geolocation and exposes cobdkit.torch, routing both to the host shell over postMessage.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "license": "AGPL-3.0",
  "scripts": {
    "build": "tsc",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.6.3"
  }
}
